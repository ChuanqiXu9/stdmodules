This is a toy example to explore possible methods to implement std modules.

It requries clang compiler and libcxx with commit ID higher than 2f1555fb11d74376347ee94
and disabling the use of `preferred_name` attribute.

Not tested with libstdc++.
